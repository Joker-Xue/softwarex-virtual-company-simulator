# Runtime dependencies for the Docker reviewer stack.
# Keep this lean so `docker compose up --build` does not pull optional AI,
# document-export, or test-only packages that are not required for smoke tests.
fastapi>=0.129.0
uvicorn[standard]>=0.35.0

sqlalchemy>=2.0.0
asyncpg>=0.31.0

python-jose>=3.4.0
bcrypt>=4.0.0
python-multipart>=0.0.20

pydantic-settings>=2.13.0
email-validator>=2.0.0
python-dotenv>=1.2.0

redis>=6.0.0
httpx>=0.28.0
